{
  "mcpServers": {
    "example-mcp": {
        "description": "Description of the server",
        "command": "command to run the mcp server like uv,python,npm etc",
        "args": [
          "Arguments to run the mcp server"
        ]
    }
  }
}